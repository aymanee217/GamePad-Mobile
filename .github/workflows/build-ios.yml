name: Build iOS

on:
  push:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    defaults:
      run:
        working-directory: GamePad-Mobile/mobile

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - uses: subosito/flutter-action@v2
        with:
          channel: stable

      - run: flutter pub get

      - run: flutter build ipa --no-codesign

      - uses: actions/upload-artifact@v4
        if: success()
        with:
          name: ios-build
          path: GamePad-Mobile/mobile/build/ios/ipa/*.ipa
